## Packages
framer-motion | Smooth layout animations and page transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes efficiently

## Notes
The API endpoints follow strict REST patterns defined in @shared/routes.
The app features a pale blue educational theme.
Quiz generation can take time, so robust loading states are required.
